[pytest]
markers =
    fast: Fast tests — no API calls, mock-based, <1s each (default)
    slow: Slow tests — real API calls, network-dependent, may take minutes
    e2e: End-to-end tests — real API + real tools, full integration
